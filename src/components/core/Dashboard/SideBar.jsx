import React from "react"
import './SideBar.css';
import { sidebarLinks } from "../../../data/dashboardLinks";

const SideBar = (props) => {
  return (
    <div>
      
    </div>
  )
};

export default SideBar;
